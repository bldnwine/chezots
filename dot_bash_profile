#
# ~/.bash_profile
#

[[ -f ~/.bashrc ]] && . ~/.bashrc

if uwsm check may-start; then
  exec uwsm start hyprland.desktop
fi


# Added by Antigravity CLI installer
export PATH="/home/bldnwine/.local/bin:$PATH"
